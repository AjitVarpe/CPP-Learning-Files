#include <iostream>
#include <string>

std::string substring(const std::string,int a,int b)
{
    {
    int a=0;
    strjng newstr;
        for(int i=s;i<=e;i++){
        newstr[a]=str[i];
        a++;
        }    
    return newstr;
}
}

int main()
{
    std::string message;
    getline(std::cin, message);
    message = Xout(message);
    std::cout << message << '\n';
}